# AIOS에 기여하기

AIOS는 로컬 AI 작업공간입니다. 공개 웹 데모는 별도의 브라우저 메모리 시뮬레이션이며 실제 AI·API·파일 작업을 수행하지 않습니다.

## 작은 변경부터

1. 문제, 재현 절차, 기대 결과를 이슈에 적습니다. 키·개인 대화·파일·로그 원문은 올리지 않습니다. 보안 문제는 [SECURITY.md](SECURITY.md)를 먼저 봅니다.
2. `main`에서 작업 브랜치를 만들고 변경 범위를 작게 유지합니다.
3. Node 22, pnpm 9.12.0으로 `pnpm install --frozen-lockfile`을 실행합니다.
4. `pnpm run doctor --demo`로 데모 개발 전제를 확인한 뒤 `pnpm demo`로 화면을 확인합니다.
5. `pnpm typecheck`, `pnpm lint`, `pnpm build`, 해당 단위 시험을 실행합니다. 데모 변경에는 `node scripts/verify-public-demo.mjs`도 실행합니다(Chromium 설치 필요).
6. PR에는 변경 이유, 실제로 실행한 검사, 실행하지 않은 검사와 이유를 적습니다. 실패·거절·충돌 경로도 시험합니다.

일반 CI는 비밀·DB·로컬 모델 없이 실행합니다. Mac/T7의 실제 API·모델·파일·복원 시험은 별도이며 CI 통과로 대체하지 않습니다. `pnpm verify`의 기본값은 타입·린트·이식 가능한 단위·빌드 4단계입니다. 단위 검사의 로컬 전용 제외와 비시험 패키지는 별도 표시되며, 선택한 시험의 0개 실행·SKIP·결과 누락은 PASS가 아닙니다. [릴리스 검증](docs/28-release-verification.md)에 범위가 명시돼 있습니다. 구형 전체/phase8은 별도 명시 옵션이 필요하며 컨테이너/DB 정리를 포함하므로 격리 환경에서만 사용합니다.

## 경계 유지

- `apps/demo`는 `apps/api`, `.env*`, 실제 파일·사용자 자료를 가져오지 않습니다. 네트워크 연결 없는 데모 경계를 유지합니다.
- 위험 작업은 승인 전 실행하지 않고, 실패를 완료로 표시하지 않습니다. 복구는 수동 변경을 덮어쓰지 않아야 합니다.
- 원래 코드에서도 새 회귀 시험이 실제 실패하는지, 또는 실패 원인을 주입해 검출되는지 확인합니다.
- `.env`·키·DB·모델·작업 폴더·백업·빌드 출력·macOS `._*`는 커밋하지 않습니다.
- 기존 사용자 변경과 데이터를 보존합니다. 지원 범위를 넘는 주장을 README에 추가하지 않습니다.

현재 라이선스는 미지정입니다. 라이선스 선택과 추가는 저장소 소유자가 결정합니다.

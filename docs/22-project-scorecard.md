# AIOS 전체 프로젝트 평가표

평가일: 2026-09-12. 기준 커밋: `f1228e5a4822ef4deaeed2201231effeb1115d0c`.

**최신 확정 평가: 90/100.** 아래 첫 표는 수정 전 기준 74점이고, 이전 개선 83점과 이번 90점의 근거를 순서대로 보존한다. 최신 재평가와 남은 10점은 문서 하단을 참고한다.

**기준점수는 74/100이다. 측정된 완성률·사용자 만족도·보안 인증이 아니라, 아래 고정 기준을 적용한 전문가 평가다.** 공개 데모의 완성도와 실제 로컬 제품의 완성도를 합쳐 과장하지 않는다. 이후 개선은 같은 항목과 배점으로 평가하며 기준 커밋의 점수를 소급해 바꾸지 않는다.

## 평가 대상과 고정 배점

대상은 공개 메인 프로젝트로서의 **로컬 AI 작업공간**이다. 대화, 참고자료, 협업, 승인된 작업 실행, 결과 검증과 복구가 핵심이다. 커널·범용 운영체제·다중 고객 SaaS는 현재 제품 범위가 아니므로, 그런 기능이 없다는 이유만으로 감점하지 않는다. 다만 지원 환경의 설치 난도와 실제 검증 공백은 감점한다.

| 범주 | 배점 | 기준점수 | 인정한 근거 | 남은 감점 근거 |
| --- | ---: | ---: | --- | --- |
| 핵심 기능 | 20 | 16 | 대화→자료→승인→실행→검증→복구 경로, 실제 모델·도구 반복 평가 | 긴 대화의 선호 기억 실패, 제한된 자료 검색·파일 형식, 지원 흐름 전체의 사용자 실사용 근거 부족 |
| 신뢰성·안전 | 25 | 20 | 작업별 승인, 해시 검증·충돌 보존 복구, 협업 종료 flush, 서명된 격리 복원 시험 | 역할별 쓰기 권한 누락과 임의 경로 색인 경계, 전원 장애·독립 디스크 재해 복원·다중 프로세스 쓰기 미검증 |
| UX·접근성 | 15 | 12 | Enter 전송·사이드바 기록·취소·시간 표시, 90개 브라우저 시나리오 중 87 PASS/3 해당 없음 | 독립 신규 사용자의 첫 실행 관찰, 보조기기·키보드·모바일 전체 흐름의 접근성 검증 부족 |
| 재현 설치·운영 | 15 | 7 | 사용·종료·진단 명령, 소스 패키지, 비밀 없는 새 복사본 의존성 설치·빌드 통과 | macOS/T7·Colima·Ollama·모델 준비 종속, 다른 Mac에서 실제 실행·복구 미검증, 같은 T7 백업의 장애 범위 공유 |
| 검증·유지보수 | 15 | 13 | 타입·린트·빌드, 결함 주입·회귀 시험, 단계별 실행 근거, 이식 가능한 CI 단위 검사 | Linux CI는 실제 DB·Docker·LLM·브라우저·T7 운영 시험을 하지 않음; 새 버전마다 전체 통합 재검증되지 않음 |
| 공개 소개·배포 | 10 | 6 | 공개 저장소, 범위·한계 문서, 비밀·대용량 업로드 제외, 비배포 CI | 설치 없는 공개 체험 부재, 데모와 실제 제품의 차이를 첫 방문에서 이해시키는 소개·운영 근거 부족 |
| **합계** | **100** | **74** | | |

64 KiB 텍스트·읽기 전용 명령처럼 안전을 위해 선언한 제한 자체가 오류는 아니다. 감점은 그 제한이 일상 작업을 얼마나 덜 완결시키는지와 지원 범위의 이해·검증 수준에 반영했다. Windows 지원, 과금, 대규모 SaaS 운영을 추가해야만 만점을 받는 기준이 아니다.

## 100점의 수용 조건 — 배점을 바꾸지 않는다

1. **핵심 기능 20점:** 선언한 대표 작업을 처음부터 끝까지 수행하고 실패를 복구할 수 있다. 고정 회귀 과제와 실제 사용 사례에 추적 가능한 결과가 있으며, 장문·자료 누락·모델 한계가 사용자에게 드러난다. 모든 질문에 정답을 보장한다는 뜻은 아니다.
2. **신뢰성·안전 25점:** 알려진 높은 위험도의 인가·파일 경계 결함이 남지 않고, viewer/member/다른 조직 및 실패 경로를 회귀 시험한다. 승인·종료·복구 계약이 실제 저장소에서 검증되며, 중요한 데이터에는 원본 디스크 상실까지 포함한 승인된 독립 백업·복구 증거가 있다. 단일 프로세스 전제 등 미지원 범위가 강제되거나 분명히 표시된다.
3. **UX·접근성 15점:** 대표 사용자 흐름이 키보드, 좁은 화면, 오류·빈 상태에서 완결된다. 접근성 자동 검사뿐 아니라 초점 이동·읽기 순서·입력·승인·복구의 수동 검증과 개발자 외 신규 사용자의 첫 사용 관찰이 있다.
4. **재현 설치·운영 15점:** 지원하는 깨끗한 다른 Mac에서 문서만으로 준비·시작·실제 대화/도구·종료·격리 복원까지 수행한 기록이 있다. 필요한 의존성·모델·경로·용량·키를 실행 전에 진단하고, 실패가 기존 데이터나 설정을 덮어쓰지 않는다. T7 지원만 선언해도 되지만 개발자 개인 설정에 의존해서는 안 된다.
5. **검증·유지보수 15점:** 이식 가능한 CI가 실제 테스트를 선택하고, 중요한 DB·파일·협업·실행 통합 계약에는 반복 가능한 별도 검증이 있다. 결함 주입이 실패를 검출하고, 모델 품질과 결정적 코드 시험을 구분하며, 해당 릴리스에 대한 최신 근거와 남은 공백이 추적된다.
6. **공개 소개·배포 10점:** 첫 방문자가 제품 범위와 설치 요건을 이해하고 공개 체험에 접근한다. 체험이 모의 응답·가상 파일이면 명시하고 로컬 데이터/API에 연결하지 않는다. 실제 배포의 주요 화면·새로고침·잘못된 경로·접근성·링크를 검사하고, 코드 실행·기여·보안 제보의 진입점 및 재사용 권한 상태를 혼동 없이 안내한다.

여기서 100점은 **명시한 범위와 평가일의 수용 조건을 충족했다는 평가**이지 무결점·미래의 보안 보장·모든 환경 지원을 뜻하지 않는다. 사용자의 계정·다른 장비·중요 데이터 이동 같은 새 권한이 필요한 항목은 승인 없이 실행하거나 점수를 채우지 않는다.

## 근거의 범위

- [1단계](17-stage1-auto-response.md): 자동 148/150, 빠른 응답 127/150의 고정 과제 평가. 특정 환경의 속도·정확도이며 일반 보증이 아니다.
- [2단계](18-stage2-daily-workspace.md): 저장 대화·자료·조직 경계 실제 API 평가. 조직 안의 개인별 비공개 프로젝트는 지원하지 않는다.
- [3단계](19-stage3-execution-safety.md): 실제 DB/Docker/API 승인·검증·복구 14/14. 외부 서비스 변경을 되돌리는 기능은 아니다.
- [4단계](20-stage4-local-operations.md): 통합 격리 복원, 종료 내구성, 사용 패키지와 브라우저 검증. 동일 T7 복원 시험은 원본 디스크가 사라진 재해 복구 시험이 아니다.
- [공개 기준](21-github-main-project.md): 비밀 없는 새 복사본 설치·타입·린트·빌드·293개 이식 가능 단위 검사. GitHub CI도 이 범위이며 실제 DB/LLM 사용까지 통과했다는 뜻이 아니다.

위 수치는 기존 검증 기록이며 이 평가 작성 시 모두 새로 실행한 결과는 아니다. 현재 커밋의 원격 결과는 [GitHub Actions](https://github.com/jonghyun0000/aios/actions)에서 해당 SHA와 함께 확인한다.

## 이번 개선에서 발견한 안전 경계

기준 커밋의 정적 코드 경로를 검토했다. 개인 파일·사용자 DB를 읽거나 실제 색인하여 재현하지 않았다.

| 문제 | 기준 코드 근거 | 필요한 개선과 성공 기준 |
| --- | --- | --- |
| 읽기 전용 viewer가 일부 쓰기를 수행 | `apps/api/src/collab-ws.ts:67`의 인증 이후 역할 검사 없음, `:107`의 Yjs 적용; `routes/workspace.ts:14,54,72,96`, `routes/core.ts:52,67`의 쓰기 역할 검사 없음 | DB/문서 변경 전에 member 이상 검증. viewer 거부와 member 성공을 모두 검사. 협업 접속 거부는 읽기 전용 협업을 구현한 것과 다름 |
| 임의 서버 폴더를 프로젝트로 색인할 수 있음 | `routes/core.ts:25`의 입력 rootDir → `worker.ts:19` → `packages/indexer/src/indexer.ts:28`, 허용 작업 폴더 경계 없음 | API와 실제 워커 실행 시 지정 작업 폴더·소유 조직·실제 경로를 검사하고 설정 부재·외부 경로·심볼릭 링크를 안전하게 거부 |

추가로 Fastify 기본 요청 로그가 WebSocket query token을 그대로 기록하는 문제를 합성 문자열과 메모리 로그로 확인했다. 실제 토큰이나 기존 로그를 출력하지 않았다. 공개 데모는 이 API를 인터넷에 노출하는 것으로 구현하지 않는다.

### 수정 및 실제 검증 — 2026-09-12

- 협업은 **member 이상만 접속**하며 viewer는 문서 참가 전에 4403, 무인증은 4401로 거부한다. 실제 인증 함수와 Fastify WebSocket을 사용하고 DB/룸만 대역으로 한 API-key·세션 시험 5개가 통과했다. 이는 viewer용 읽기 전용 협업을 구현한 것이 아니라 접속 자체를 제한한 안전 패치다.
- 새 Yjs 문서와 별개인 구형 `/v1/ws` 릴레이도 `collab.update`만 member 이상으로 제한했다. viewer의 자기 조직 이벤트/구형 협업 읽기는 유지하며 잘못된 JSON 프레임의 null/배열/비문자열 필드를 거부한다. 독립 검토자가 기존 코드에서 viewer 발행·null 프레임 2개 실패를 확인했고, 패치 후 역할·조직 경계 포함 7개 시험이 통과했다.
- 세션 생성·변경·자료 연결/해제와 메모리 저장/삭제는 member 이상을 요구한다. viewer 거부 6개, member 정상 쓰기 6개, viewer 읽기 유지 1개가 통과했다. 패치 전 동일 viewer 시험 6개는 실제 라우트가 200을 반환하여 실패했다.
- 채팅 전송도 도구 사용 여부와 무관하게 member 이상을 요구한다. 기존 코드는 도구를 끈 viewer가 실제 오케스트레이터를 거쳐 모델 대역 1회·메시지 저장 대역 2회에 도달했다. 합성 회귀 13개 중 viewer 기본 전송/API-key·세션 및 auto/fast/thorough 모드 5개가 기존 코드에서 실패했고, 진입부 역할 검사 적용 후 13개 모두 통과했다. member의 정상 모델/저장 흐름, 다른 조직 대화의 404, viewer의 조직 범위 안 읽기도 유지한다. 실제 사용자 DB·LLM은 사용하지 않았다.
- 색인은 API 접수와 워커 실행 양쪽에서 프로젝트 소유권, 전용 폴더의 로컬 조직 소유권, `LOCAL_WORKSPACE_ROOT`, 실제 경로를 확인한다. 설정 없는 배포에서는 409로 중단하며 외부 폴더·링크 경로는 403이다. 워커는 `.gitignore`와 각 본문 읽기 전에 경로/링크 검증을 반드시 전달한다. 인덱서의 독립 라이브러리 호출은 기존 호환성을 유지하므로 API/워커 우회 사용자의 별도 경계까지 보장하지 않는다.
- 색인 API/워커 FS·DB 대역 시험 11개, 인덱서 6개(기존 청커 3개 포함)가 통과했다. 경로 검사와 워커 callback을 잠시 제거한 결함 주입에서 6개 실패, 인덱서 읽기 전 검증을 제거하면 2개 실패를 확인한 뒤 안전 코드를 복원했다. 링크/하드링크 결함은 FS 대역으로 검증했으며 T7의 물리적 링크 시험이라고 주장하지 않는다.
- 실제 T7에 독점 생성한 일반 텍스트 fixture에서도 지정 폴더의 내용만 임베딩 대역으로 전달되고 인접 폴더는 거부됐다. 실제 사용자 DB/LLM/개인 파일은 사용하지 않았다. `AIOS_INDEX_FS_TEST=1`일 때만 실행되는 별도 시험 1개이며 기본 Linux CI에서는 명시적으로 건너뛴다. 합성 fixture만 정리했다.
- 서버 로그는 쿼리 전체를 버리고 인증/쿠키 헤더를 가리며, 외부 Error의 message/stack을 고정 문자열로 바꾸고 config/cause를 기록하지 않는다. API/worker 초기화 실패와 worker 작업 실패도 허용된 오류 종류·코드·상태만 남긴다. HTTP/오류 요약 4개와 실제 진입점에 합성 실패를 주입한 3개 시험이 통과했다. 기존 원문 로그를 잠시 복원하면 진입점 시험 3개가 모두 실패함을 확인한 뒤 안전 코드를 복원했다. 요청 ID·경로와 정형 오류 정보는 남지만 상세 원인에는 비밀 없는 별도 계측이 필요하다. 기존 로그 검토/삭제나 실제 키 폐기는 하지 않았다. **WebSocket URL 토큰 방식 자체, 외부 프록시 로그, 프로젝트의 모든 별도 도구/프로세스 출력에 대한 전면 감사는 해결 범위가 아니다.**

위 수정 후 API 시험은 18개 파일 **110 PASS**였다. 기존 T7 전용 `checkpoints.test.ts`와 `local-operations.test.ts`는 이 실행에서 제외했고 새 T7 색인 시험 1개는 명시적으로 실행했다. 인덱서 **6 PASS**, API/인덱서 타입·린트와 diff 공백 검사가 통과했다. 이 결과는 실제 DB/Docker/LLM 통합 전체를 새로 실행한 결과가 아니다.

## 개선 후 재평가 기록

**기준점수는 74점으로 고정한다. 목표는 위 수용 조건의 100점이며, 이번 공개 데모와 설치 진단 개선만으로 전체 100점을 달성했다고 하지 않는다.**

안전 수정과 회귀 시험만 반영한 중간 평가는 **77/100**이었다. 읽기 전용 설치 진단(17개 자체 검사 통과), 데모 준비물 확인, [처음 복제한 사람의 안내](23-getting-started.md), [기여 안내](../CONTRIBUTING.md)와 [보안 제보 안내](../SECURITY.md)를 추가한 뒤 **80/100**이었다. 최종 정적 체험판을 실제 공개 배포하고 공개 소스·문서 링크까지 확인한 재평가는 **83/100**이다. 구현 커밋은 `d5966f9`이며 점수는 독립 검토자가 같은 고정 배점으로 재확인했다.

| 범주 | 기준 | 현재 | 변화의 근거 |
| --- | ---: | ---: | --- |
| 핵심 기능 /20 | 16 | 16 | 실제 제품의 대표 기능·기억 품질을 이번에 확장했다고 계산하지 않음 |
| 신뢰성·안전 /25 | 20 | 22 | 실제 확인한 역할·색인·오류 로그 경계 수정과 실패 검출 |
| UX·접근성 /15 | 12 | 12 | 별도 데모의 화면 개선을 전체 로컬 앱 접근성 검증으로 대체하지 않음 |
| 재현 설치·운영 /15 | 7 | 9 | 비밀 출력/자동 변경 없는 진단과 데모/전체 앱을 분리한 설치 안내; 다른 Mac 실기동은 아직 없음 |
| 검증·유지보수 /15 | 13 | 14 | 새 코드 경계의 실제 회귀·결함 주입·합성 파일 검사 |
| 공개 소개·배포 /10 | 6 | 10 | 무인증 공개 체험·정확한 모의 범위 안내·보안 헤더·빌드 동일성·브라우저/접근성·공개 소스/문서 진입점 검증 |
| **합계** | **74** | **83** | 동일한 근거를 여러 범주에 중복 가산하지 않음 |

[공개 체험판](https://aios-demo-mu.vercel.app)은 로그인 없는 HTTP 200, 전체 보안 헤더·404, 데스크톱/모바일 24개 검사와 공개 4개 파일의 길이/SHA256 일치가 확인됐다. 같은 빌드의 접근성은 로컬 13개 화면 자동 위반 0, 키보드/레이아웃 40개 확인 통과다. `d5966f9` 공개 후 저장소·데모 소스·안전 실행·평가·시작·체험 문서·기여/보안 안내의 8개 링크가 실제 HTTP 200으로 열렸다. [배포 검증 기록](24-public-demo.md)을 참고한다. 별도 데모 검증은 실제 로컬 제품 UX·모델 품질의 만점 근거가 아니다.

**남은 17점:** 핵심 기능 4, 신뢰성·안전 3, 전체 앱 UX 3, 재현 설치·운영 6, 통합 검증·유지보수 1. 앞서 정의한 장문/자료·대표 사용 흐름, 실제 장애·독립 백업 복구, 신규 사용자·보조기기 관찰, 깨끗한 다른 Mac 설치/대화/종료/격리 복원, 최신 전체 통합 근거를 채워야 한다. 점수표를 느슨하게 바꾸거나 검사 수만 늘려서 메우지 않는다.

수정 후 점수는 구현·실제 실행·결함 주입·배포 확인이 끝난 항목만 반영한다. 다른 Mac 실사용, 독립 매체의 중요 데이터 복구, 사람의 접근성/신규 사용자 관찰처럼 아직 없는 근거는 미충족으로 남긴다. 확인 전의 예상 점수를 최종 점수로 쓰지 않는다.

### 최종 코드의 추가 회귀 근거

최종 API 패치 후 기존 사용자 시작기로 정상 종료·재기동했다. 실제 로컬 웹 회귀는 **87 PASS / 0 FAIL / 3 SKIP**, 재시도 0, 133.56초였다. SKIP은 모바일 전용 3개의 desktop 실행이며 실패를 생략한 것이 아니다. 대화 관리 시험은 고정 제목의 첫 항목 대신 시험이 만든 세션 ID만 선택하도록 고쳐 동명의 사용자 대화를 보호했다. 중복 제목 결함 주입으로 이전 선택자의 잘못된 대상을 확인했다.

실제 준비 상태는 API·worker·Postgres·Redis 정상, doctor 14항목 확인, 고아 esbuild 0이었다. 이 실행에서 기존 DB 초기화·사용자 자료 교체·실제 통합 백업/복원은 하지 않았다. 브라우저 승인/복원 표시 검사 일부는 합성 응답을 사용하므로 실제 파일 복원 통합 시험으로 계산하지 않는다.

`9a5d150`의 [GitHub CI](https://github.com/jonghyun0000/aios/actions/runs/34685116460)는 실제 Linux runner에서 의존성 설치·타입·린트·빌드·이식 가능한 단위·doctor/데모 정책·정적 데모 Chromium 검사까지 성공했다. 로컬 실서비스 검사와 원격 정적 데모 검사를 합쳐 클라우드 실제 AI 서비스 검증이라고 주장하지 않는다. 이 근거는 위 83점의 회귀 확인이며, 독립 장비/사용자의 검증 공백을 없애는 추가 점수로 계산하지 않았다.

## 실제 제품 개선 후 재평가 — 2026-09-12

구현 커밋 `45636ce313e7a2afa16995ada09b964dcbf9b4d3`의 [실제 GitHub CI 성공](https://github.com/jonghyun0000/aios/actions/runs/34689809471)까지 확인한 뒤 **90/100으로 확정했다.** 기준 74점과 이전 개선 83점은 그대로 보존한다. 배점·수용 조건을 바꾸거나 공개 데모를 실제 제품으로 바꾸어 세지 않았다. 구현 담당과 분리한 AI 검토가 실제 보고서, 고정 과제·하네스와 커밋의 코드 지문, CI의 동일 SHA·성공 상태를 재검산했다. 이는 정해진 범위에 대한 평가이지 보안 인증이나 사용자 만족도 측정이 아니다.

| 범주 | 이전 / 배점 | 이번 확정 | 인정한 개선 | 남은 감점 |
| --- | ---: | ---: | --- | --- |
| 핵심 기능 | 16 / 20 | 19 | 오래된 명시 선호·최신 수정·초기화·기억 끄기, 한국어 자료 검색과 실제 전달 출처, 현재 작업의 파일 형식 구분. 같은 대표 자료 업무3건이 승인·검증·복구까지 완료 | 1: 제한된 선호/자료 형식·기억 범위와 모델의 일반화 한계. 임의 장문 사실을 모두 기억하거나 모든 질문에 정답을 보장하지 않음 |
| 신뢰성·안전 | 22 / 25 | 23 | workspace별 단일 API 강제와 백업 유지보수 상호 배제, 복구 중 파일 성공/DB 응답 실패의 재시도·일관된 기록, 제3자 변경 보존 | 2: 물리적 전원 장애·원본 디스크 상실·독립 매체 재해 복원 근거 없음. 다른 workspace의 API가 같은 DB를 공유하는 구성/분산 실행 미지원 |
| UX·접근성 | 12 / 15 | 14 | 실제 앱의 키보드 읽기·모달 초점·승인 오류 재시도·상태/기억 범위 안내·작은 노트북 창 대화 목록을 구현하고 검사 | 1: 개발자 외 신규 사용자 관찰·실제 보조기기 검증 없음. axe 색상 대비 incomplete를 수동 해결했다고 계산하지 않음 |
| 재현 설치·운영 | 9 / 15 | 9 | 기존 준비물 진단·시작기 근거 유지. 다른 M1을 확보했다는 사실 자체는 가점 아님 | 6: 깨끗한 다른 Mac에서 문서만으로 설치·실제 대화/도구·종료·격리 복원 미검증; T7·모델·컨테이너 준비 종속 |
| 검증·유지보수 | 14 / 15 | 15 | 검사0개/내부SKIP/옛 JSON/선택 누락을 PASS로 세지 않는 검증기, 동결 코드 지문·명시 CI 범위·실제 통합 하네스·결함 주입과 실패 이력 추적. 동일 구현 커밋의 실제 CI 성공 확인 | 이 항목의 선언 범위 충족. 다른 장비·복구·사람 관찰 공백은 해당 범주에 남김 |
| 공개 소개·배포 | 10 / 10 | 10 | 기존 공개 체험·범위 안내·소스/기여/보안 진입점 유지 | 이번에 체험판을 재배포한 것으로 계산하지 않음 |
| **합계** | **83 / 100** | **90** | 각 개선은 해당 범주에만 가산 | **10점의 공백은 남음** |

### 최신 실행 근거와 실패 보존

- [맥락·자료](25-context-and-evidence.md): 동일 고정8과제×3회. 범용 현재 턴 정책 이후 실제 로컬 모델+DB 대역은24/24, 정상 재시작 뒤 실제 API·Postgres·Redis까지 연결한 별도 실행도24/24다. 두 실행을 합쳐 일반 정확도48/48이라고 주장하지 않는다. 이전18/24·23/24와 선호가 우연히 맞은 경우를 함께 기록했다.
- [대표 업무](29-representative-workflow.md): 같은 하네스·승인 조건의 최종3/3. 실제 자료/출처·선호→파일 제안→명시 승인→읽기 전용 검증exit0→호스트 readback/해시→복구/DB기록→새 대화만 휴지통의 전 경로를 확인했다. 최초0/3과 개선 전 후보2/3의 실패를 보존했고, 잘못된 형식은 승인 전에 실제 거절됐다. 답안·승인 조건 완화나 자동 재시도로 성공 수를 채우지 않았다.
- [실행 내구성](26-durable-execution.md): 격리 내구성25/25(실제 새 DB9·프로세스 소유권13·DB URL 방어3), 별도 실제 T7 색인1/1. 기존 승인·검증·복구 회귀14/14와 실제 중복 API 기동 거절도 기록했다. 같은 T7의 합성 파일을 독립 디스크 재해 복원으로 바꿔 부르지 않는다.
- [실제 앱 접근성](27-local-app-accessibility.md): 합성 API/WS의36/36, axe48회 확정 위반0. 이 중46회/358대상 출현의 색상 대비는 자동 판정 불완전이며 사람이 모두 확인한 결과가 아니다. 전체 브라우저는123 PASS/0 FAIL/3desktop 해당 없음, 재시도0으로 실제 API 검사와 합성 검사가 섞여 있다. 두 수치는 일부 중복되어 합산하지 않는다.
- [릴리스 검증](28-release-verification.md): 동결한 코드의 타입·린트·이식 가능 단위450·운영 결함 주입·격리 내구성25·색인1·빌드7단계 PASS. 전후 코드 지문 `25cbbe0fca9fbab77efbbfbd547cd71e51c4b1510e556f96132565f90b88dfae`는 구현 커밋과 일치한다. 앞서 코드 수정 중의 `SOURCE_CHANGED` 결과는 최종 PASS로 재사용하지 않았다. API 특수5파일 제외/비시험5패키지는 통과 수와 별도로 보고한다.
- 같은 구현 커밋의 Linux CI는 실제 단위450/450, 진단·검증기 경계51개, 정적 데모24개, 실제 앱의 합성 API 접근성36/36을 통과했다. 접근성 실행의 FAIL/SKIP/flaky/재시도는0이며 전체 작업은2분58초였다. CI는 로컬 DB·모델·T7·독립 디스크 복원 시험을 대신하지 않는다.

사용자의 다른 Mac은 **M1 MacBook Air 13인치·16GB RAM·256GB 저장공간**이다. 이 장비의 사양 전달은 설치·성능·용량·복구의 실제 성공 증거가 아니다. 그 장비에서의 다음 단계와 독립 디스크/사람 관찰이 남은 10점을 줄일 후보이며, 현재 Mac의 결과로 대신 채우지 않는다.

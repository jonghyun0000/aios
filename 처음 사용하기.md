# AIOS 처음 사용하기

이 Mac과 T7에 준비된 로컬 실행판입니다. 외부 AI API 키나 결제 없이 사용합니다.

## 시작과 종료

1. T7 외장 디스크를 연결합니다.
2. 이 프로젝트 폴더의 **AIOS 시작.command**를 더블클릭합니다.
3. ‘AIOS 준비 완료’가 나오면 브라우저가 자동으로 열립니다. 로그인은 필요 없습니다.
4. 사용 중에는 시작 터미널을 열어 두세요. 종료하려면 **AIOS 종료.command**를 더블클릭하거나 시작 터미널에서 **Ctrl+C**를 누릅니다. ‘종료 완료’를 확인하세요.

브라우저가 열리지 않으면 `http://127.0.0.1:8791`로 접속하세요. 개발용 서버의 8790 포트와 다릅니다.
시작 파일을 다시 실행했을 때 이미 켜져 있으면 기존 화면을 엽니다. 재부팅 후에도 같은 파일로 시작합니다.
**AIOS 상태 확인.command**는 현재 앱·워커·DB 준비 상태와 로그 위치를 알려줍니다. 소스·설치 의존성·산출물이 그대로면 빌드를 재사용합니다.

## 먼저 해볼 것

- **채팅:** ‘오늘 할 일을 정리하는 방법을 세 문장으로 알려줘’처럼 질문해 보세요.
- **전송:** Enter로 보내고, Shift+Enter로 줄바꿈합니다. 한글 조합을 확정하는 Enter는 전송하지 않습니다.
- **대화 내역:** 왼쪽 메뉴 아래에서 대화를 이어가거나 ‘+ 새 대화’를 누르세요. 모바일에서는 상단 ‘대화 내역’을 펼칩니다.
- **검색·정리:** ‘제목·본문 검색’에 단어를 입력하세요. 대화 오른쪽 **⋯**에서 이름을 바꾸거나 삭제할 수 있습니다.
  삭제는 휴지통 이동이며 영구 삭제가 아닙니다. **휴지통 → 대화 선택 → 대화 복구**로 되돌릴 수 있습니다.
- **응답 모드:** 기본 ‘자동 선택’은 간단한 질문은 빠르게, 명확한 사칙연산은 정확 계산으로, 코드·복잡한 질문은 깊이 생각으로 처리합니다.
  ‘빠른 응답’과 ‘깊이 생각’을 직접 선택할 수도 있습니다. 자동 분류는 규칙 기반이며 질문의 복잡성을 항상 알아내지는 못합니다.
  정확 계산은 사칙연산·괄호·소수를 지원합니다. 단위·백분율·서술형 수학 문제는 모델 추론으로 넘기며 정답을 보장하지 않습니다.
- **시간 확인:** ‘이번 응답 시간’을 펼치면 모델 적재·입력 처리·추론/생성·앱 대기열·기억 검색 시간을 볼 수 있습니다.
  정확 계산은 모델을 호출하지 않습니다. 시간 상세는 이번 화면의 마지막 응답에만 남고 새로고침하면 지워집니다.
- **공공통계:** 메뉴에서 통계 이름을 검색하고, 상세 화면의 지역·항목 필터와 차트를 확인하세요.
- **파일 만들기:** 채팅 입력창 위의 **도구 사용 허용**을 켜고 아래처럼 요청하세요.

> hello.js 파일에 console.log('안녕하세요'); 를 저장한 뒤 node hello.js로 실행하고 출력값을 알려줘.

생성된 파일은 Finder에서 아래 폴더를 열어 확인합니다.

`/Volumes/T7/bigdata/workspaces/my-first-project`

도구를 켜면 이 작업 폴더의 파일을 읽을 수 있고, **수정과 명령 실행은 매번 승인**해야 합니다. 중요한 원본을 넣기 전에는 복사본으로 시험하세요.
명령 실행은 네트워크 차단·작업 폴더 읽기 전용 Docker 샌드박스에서 이루어집니다. 명령으로 파일을 수정할 수 없으며, 임시 출력은 `/tmp`를 사용합니다.
인터넷 패키지 설치나 작업 폴더에 산출물을 쓰는 빌드는 이 단계의 지원 범위 밖입니다.
작업 폴더 밖의 기존 프로젝트를 지정하는 UI는 이 로컬 실행판에 포함하지 않았습니다.

## 자료를 연결해 대화하기 — 2단계

1. 채팅 오른쪽 위 **자료·프로젝트**를 누릅니다. 아직 대화가 없으면 빈 대화를 만듭니다.
2. 프로젝트 이름을 입력해 새 프로젝트를 만들거나 기존 프로젝트를 선택합니다. 프로젝트 없이 대화별 자료만 연결해도 됩니다.
3. 파일 적용 범위를 **이 대화에만** 또는 **프로젝트 공용**으로 고르고 파일을 선택합니다.
4. 창을 닫고 ‘참고자료에서 배포 식별값을 찾아줘’처럼 질문하세요. 도구 사용을 켜지 않아도 참고할 수 있습니다.
5. 다른 대화에 같은 프로젝트를 연결하면 공용 자료도 이어집니다. 파일의 **해제**는 원본 파일을 지우지 않으며, 공용 파일 해제는 모든 연결 대화에 적용됩니다.

지원: UTF-8 텍스트·Markdown·JSON·CSV·일반 코드 파일, **파일당 64KB / 대화·프로젝트 각각 8개**.
PDF·이미지·Word는 아직 지원하지 않습니다. 파일의 텍스트 사본을 저장하므로 원본 수정은 자동 반영되지 않습니다.
질문에 관련된 구간을 최대 4개 골라 모델 입력 한도 안에서 참고합니다. 전체 파일을 항상 읽는 기능은 아닙니다.
비밀 키·비밀번호가 담긴 자료는 연결하지 마세요. 숨김 파일과 일부 비밀 파일 이름을 막지만 모든 비밀을 탐지하지는 못합니다.

프로젝트는 **대화와 참고자료를 묶는 공간**입니다. 컴퓨터의 실제 프로젝트 폴더를 자동 연결하지 않으며, 도구의 작업 폴더도 기존 전용 폴더 그대로입니다.

## 안전하게 실행·검증·복구하기 — 3단계

1. **도구 사용 허용**을 켭니다. 실제 테스트가 있다면 **검증 명령**에 `node --test test.js`처럼 입력하세요. 명령이 없으면 동작 미검증으로 남습니다.
2. 작업을 요청하면 입력창 위 **실행 기록**이 펼쳐집니다. 파일 경로와 변경 전·후 전체 내용, 또는 명령과 실행 위치를 확인합니다.
3. **이번 작업 승인**은 표시된 1건에만 적용됩니다. 아직 실행하지 않은 상태에서 기다리며, **거절하고 중단** 또는 5분 만료 시 실행하지 않습니다.
4. 파일 쓰기 뒤 실제 내용을 다시 읽어 SHA-256을 비교합니다. 명령은 실제 종료 코드와 출력을 남깁니다. 종료 코드가 0이 아니면 실패입니다.
5. AI 답변의 ‘완료’와 시스템 검증은 별개입니다. **지정 검증 통과**는 입력한 명령이 당시 종료 코드 0으로 끝났다는 뜻이며, 모든 요구사항의 정확성을 보장하지 않습니다.
6. **실행 기록 → 이 변경 복구 → 확인하고 복구**로 그 파일의 변경을 되돌립니다. 새로 만든 파일은 제거하고, 기존 파일은 직전 내용으로 복구합니다.
   그 뒤 직접 수정한 파일은 충돌로 중단하여 덮어쓰지 않습니다. 여러 번 수정했다면 최신 변경부터 복구하세요. 복구 후에는 다시 검증해야 합니다.

파일 편집은 **최대 64 KiB UTF-8 일반 파일**만 지원합니다. 숨겨진 `.git` 경로, 심볼릭 링크·하드 링크는 파일 도구에서 거부합니다.
파일 삭제·이동·권한 변경·패키지 설치·DB 변경·외부 서비스 취소를 포괄하는 복구 기능은 아닙니다.
읽기 전용 명령이 반환한 출력에는 숨겨진 비밀이 있을 수 있으니 작업 폴더에 비밀 키를 넣지 마세요.

파일 백업은 `/Volumes/T7/bigdata/checkpoints/stage3`에, 승인·실행 기록은 기존 Postgres에 남습니다. 원본 소스 전체 백업과는 별개입니다.
새로고침·중단·서버 재시작이 승인을 자동 재사용하지는 않습니다. 실행 확인이 끊긴 경우 기록과 실제 파일을 확인하세요.
현재는 **단일 로컬 API**용입니다. 다른 프로그램이 동시에 작업 폴더를 바꾸지 않도록 해 주세요.

## 백업하고 복원 가능 여부 확인하기 — 4단계

1. **AIOS 종료.command**로 앱을 정상 종료합니다. 다른 개발 서버나 DB 도구도 닫고, 작업 폴더 편집을 멈춥니다.
2. **AIOS 백업.command**를 더블클릭합니다. 성공한 백업은 `/Volumes/T7/bigdata/backups/local`에 남습니다.
3. **AIOS 복원 검사.command**를 더블클릭합니다. 최신 백업을 **새 DB·새 폴더**에 복원하고 실제 내용과 파일 복구 동작을 검사합니다. 기존 데이터는 교체하지 않습니다.
4. **AIOS 시작.command**로 다시 시작한 뒤 **설정 → 내 컴퓨터 운영**에서 최근 기록을 확인합니다.

대화·연결 자료·협업 문서·현재 작업 폴더·변경 복구 기록을 함께 백업합니다. 모델·대용량 통계·Redis 대기 작업·다른 작업 폴더·별도 비밀 설정은 제외합니다.
서명 키 `/Volumes/T7/bigdata/secrets/local-backup-signing.key`도 별도로 안전하게 보관하세요. 없으면 기존 백업을 검증·복원할 수 없습니다. 백업 안에는 대화/파일의 민감한 내용이 들어갈 수 있습니다.
같은 T7의 백업은 T7 고장·분실까지 보호하지 않습니다. 별도 디스크에 보관할 때는 키와 백업을 암호화하여 안전하게 보관해야 합니다.

**AIOS 사용 패키지 만들기.command**는 코드·실행 스크립트·설명서를 재설치용 소스 묶음으로 만듭니다. DB 백업과는 별개이며, 다른 컴퓨터에서 바로 실행되는 설치 앱이 아닙니다. Node 22·pnpm 의존성·Docker/Colima·Ollama·모델·로컬 설정이 별도로 필요합니다.
검사에 실패하면 부분 백업이나 기존 데이터를 지우지 말고 오류와 **상태 확인.command** 결과를 확인하세요. 복원된 검사본을 운영 데이터로 바꾸는 작업은 자동 수행하지 않습니다.
범위와 상세 복구 절차는 `docs/20-stage4-local-operations.md`에 있습니다.

## 알아두면 좋은 점

- 로컬 8B 모델이라 모델을 처음 불러올 때나 ‘깊이 생각’, 여러 단계의 파일 작업에는 시간이 걸릴 수 있습니다. 생성 상태와 경과 시간이 표시됩니다.
- **중단**을 누르면 응답 생성과 진행 중인 도구에 취소를 전달합니다. 이미 저장된 파일까지 되돌리지는 않습니다.
- 대화는 데이터베이스에 저장되어 다시 시작한 뒤에도 대화 목록에서 열 수 있습니다.
- 2단계 웹 채팅은 임시 캐시가 만료되어도 저장된 최근 **100개 메시지**를 불러옵니다. 이는 100턴이 아니라 사용자·AI 메시지 합계입니다.
  모델 입력 한도를 넘으면 그중 오래된 일부도 제외될 수 있고 화면에 안내합니다. 모든 과거 대화를 무제한 회상하는 기능은 아닙니다.
  중요한 내용은 다시 알려주거나 참고 파일로 연결하세요. 원본 대화 기록은 삭제하지 않습니다.
- 통계 엔진이 예기치 않게 종료되면 오류가 표시됩니다. 다시 조회하면 엔진이 재시작됩니다.
- 이 실행판의 결제 화면에는 로컬 사용 안내가 나옵니다. OAuth·Stripe 실계정·클라우드 운영은 별도 검증 범위입니다.
- 모델의 답변과 코드는 틀릴 수 있으므로 실제 용도로 쓰기 전에 결과를 확인하세요.
- 1단계 검증은 자동 모드 148/150 통과였으며 지정 언어 회상 오류가 남아 있습니다. 실제 검증 조건과 시간은 `docs/17-stage1-auto-response.md`에 기록했습니다.
- 2단계 구현 범위와 검증은 `docs/18-stage2-daily-workspace.md`에 기록했습니다. 자료와 대화는 기존 로컬 Postgres에 저장되므로 T7 소스 폴더만 복사하는 것으로 DB 백업을 대신할 수 없습니다.
- 3단계 구현 범위·결함 주입·실제 모델 왕복 검증은 `docs/19-stage3-execution-safety.md`에 기록했습니다.
- 4단계 기동·종료·통합 백업과 격리 복원 검사는 `docs/20-stage4-local-operations.md`에 기록했습니다.

## 시작에 문제가 있을 때

- T7가 연결되어 있는지 확인하고 `AIOS 시작.command`를 다시 실행합니다.
- 터미널에 표시된 오류와 로그 위치를 확인합니다. 로그는 `/Volumes/T7/bigdata/logs/local-app`에 있습니다.
- **AIOS 상태 확인.command**로 확인하고, 실행 중인 앱은 **AIOS 종료.command**로 종료한 뒤 다시 시작하세요. 다른 프로그램이 포트를 쓰고 있으면 자동 종료하지 않습니다.
- 시작 파일이 실행되지 않으면 프로젝트 폴더를 터미널에서 열고 `bash 'AIOS 시작.command'`를 실행할 수 있습니다.
- 앱 종료 뒤에도 DB·Redis·Ollama는 유지됩니다. T7를 분리하기 전에는 다른 사용 작업을 마친 뒤 해당 서비스도 안전하게 종료하세요. 비정상 종료나 유지보수 잠금을 임의 삭제하지 마세요.

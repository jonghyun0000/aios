# 패키지가 선언한 Node 버전 요구를 설치 시점에 강제한다.
#
# 왜: undici@8(Node>=22.19 요구)을 별생각 없이 추가했는데, 호스트(22.18)에서는 우연히 돌고
# 컨테이너(Node 20)에서만 `markAsUncloneable is not a function` 으로 죽었다.
# 즉 **런타임에, 그것도 배포 환경에서만** 드러났다.
# engine-strict 면 설치 단계에서 바로 막힌다 — 훨씬 싼 자리에서 실패한다.
engine-strict=true

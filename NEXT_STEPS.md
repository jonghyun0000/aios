# NEXT_STEPS — 아스트라 작업 가이드라인

> 작성 2026-09-19. 읽는 순서: [AGENTS.md](AGENTS.md) → [HANDOFF.md](HANDOFF.md)(현재 상태·기동·함정·위험) → **이 문서**(무엇을, 어떤 순서로, 어떻게 끝낼지).
> 무엇이 검증됐는지는 HANDOFF 와 `docs/` 의 실행 기록을 따른다. 작업 순서와 방법은 이 문서를 따른다.
> 작업을 하나 끝낼 때마다 §5 표의 **상태** 열을 갱신한다.

---

## 1. 출발점 — 냉정한 기준

| | 값 | 근거 |
|---|---|---|
| 저장소의 자체 평가 | 90/100 | `docs/22-project-scorecard.md` (고정 배점, AI 검토) |
| 2026-09-19 별도 점검의 추정 | **약 70/100 (±6)** | 같은 6범주 배점을 적용한 의견. 공식 점수가 아니다 |

두 점수가 다른 이유는 아래 네 가지다. 다음 재평가는 이 네 가지에 각각 답해야 한다.

1. **채점 범위를 먼저 좁혔다.** 설계 문서(`docs/01`)는 라우터·플러그인·과금을 갖춘 "AI 운영체제"다. 평가표는 범위를 "로컬 AI 작업공간"으로 다시 정의한 뒤 채점했고, 채점자도 AI 다.
2. **외부 사용자가 0명이다.** 별 1개, 이슈·PR 0건, 라이선스 없음, 신규 사용자 관찰 0회.
3. **같은 과제로 조정하고 같은 과제로 쟀다.** 맥락 평가는 같은 8과제로 18 → 23 → 24/24가 됐다. 대표 업무는 n=3이고, 모델은 qwen3:8b 하나다.
4. **점수표 밖의 결함이 있었다.** 의존성 취약점 15건(해소됨), 운영 이미지 빌드 불가, 검증 하네스 오류, 오류 분류 잡음이다(HANDOFF §7 위험 30~32).

**요약: 검증 규율은 강하지만 폐루프다.** 개발자도 기계도 과제도 매번 같았다. 제품 코드 약 13.9K줄에 검증 하네스가 약 8.8K줄이다.
다음 단계의 목적은 점수를 올리는 것이 아니다. **다른 환경, 다른 사람, 보지 않은 과제**라는 근거로 이 루프를 여는 것이다.

---

## 2. 바뀐 전제 — 두 번째 Mac이 없다

2026-09-19 사용자가 M1 MacBook Air(13", 16GB/256GB)를 판매했다. 남은 실기기는 이 개발 Mac(M4 16GB)과 T7 한 대뿐이다.

**규칙**
- 평가표 수용 조건 4의 "깨끗한 다른 Mac" 근거는 **현재 충족 불가**로 남긴다. 기준을 완화하지 않고, 다른 근거로 채우지도 않는다.
- 아래 대체 근거의 결과는 **"대체 근거"로 따로** 기록하고 점수에 더하지 않는다.
  점수에 반영하려면 사용자가 평가표 개정을 명시적으로 승인해야 한다. 개정은 이전 판을 보존하고 날짜를 붙인 새 판으로 한다.
- 대체 근거를 실행하기 **전에** 무엇을 입증하고 무엇을 입증하지 못하는지 적는다. 아래 표를 그대로 쓰면 된다.

| 대체 | 방법 | 입증하는 것 | 입증하지 못하는 것 | 필요한 것 |
|---|---|---|---|---|
| **A. 새 macOS 계정 클린룸** | 같은 Mac에 새 표준 계정을 만든다. 그 계정에서 README 와 `docs/23` 만 보고 설치 → 시작 → 대화 1회 → 도구 승인 1회 → 종료 → 격리 복원 검사까지 진행한다 | 개발자의 홈·PATH·전역 npm/corepack·git 설정·키체인에 기대지 않는다. Colima VM 이 사용자별이라 Docker 도 새로 준비된다. 문서의 빈틈이 드러난다 | 다른 하드웨어·macOS 버전·다른 T7, 첫 모델 다운로드(모델은 T7 공유) | **계정 생성은 사용자만 할 수 있다**(에이전트 금지 작업). 진행은 사용자 본인이 하거나, 그 계정에서 새 에이전트 세션을 연다 |
| **B. Linux CI 통합 잡** | GitHub `ubuntu-latest`(공개 저장소 기준 4코어/16GB)에서 Postgres(pgvector)·Redis·Ollama 를 서비스 컨테이너로 띄운다. 마이그레이션 → API 기동 → 대화 1회 → 승인 흐름 → readback 해시 순서로 확인한다 | 매 커밋마다 깨끗한 기계에서, 문서화된 의존성만으로 핵심 경로가 돈다. 현재 CI 의 가장 큰 공백(실제 DB·모델 미사용)을 메운다 | macOS·T7·exFAT·Colima 경로, 8B 모델의 품질 | 없음. 모델 다운로드 시간과 캐시는 설계해야 한다 |
| **C. macOS arm64 러너 스파이크** | GitHub `macos-14`/`macos-15`(M1 3코어/7GB, 매번 새 환경). `hdiutil` 로 `T7` 이라는 이름의 exFAT 이미지를 만들어 `/Volumes/T7` 에 마운트한다. 그 위에서 설치·doctor·빌드·단위 시험을 돌린다 | 깨끗한 M1 macOS + exFAT 에서 설치와 진단이 정확한지 확인한다(`._*` 사이드카, NFD 파일명 포함) | **전체 앱은 띄울 수 없다.** GitHub 문서에 따르면 arm64 macOS 러너는 중첩 가상화를 지원하지 않아 Colima/Docker 를 쓸 수 없다. 7GB 로는 8B 모델도 돌릴 수 없다 | 없음. 1시간 상한 스파이크로 가능 여부부터 확인해 기록한다 |
| **D. 신규 사용자 관찰 겸 설치** | 외부 사람 1~3명이 이 Mac 의 A 계정에서 문서만 보고 설치하고 처음 사용한다. 사용자가 옆에서 막힌 곳과 걸린 시간을 기록한다 | 처음 보는 사람도 이해할 수 있는지 확인한다. 수용 조건 3·4 양쪽의 가장 큰 공백이다 | 다른 하드웨어 | 사용자의 섭외 |

권장 순서: **B**(자동이고 반복 가능) → **A**(사용자 30~60분) → **D**(섭외되면 A 계정에서) → **C**(여유가 있을 때).

---

## 3. 일하는 방법 — 모든 작업에 적용

### 3.1 세션을 시작할 때
1. `git status` 가 깨끗한지 확인한다. 변경이 남아 있으면 무엇인지 사용자에게 알린다. 남의 미완 변경 위에 작업을 쌓지 않는다.
2. 맡을 항목의 현재 상태를 HANDOFF §7 과 §5 표에서 확인한다. 다른 세션이 이미 고쳤을 수 있다.
3. 환경을 준비한다. 자세한 내용은 HANDOFF §3·§8 에 있다.
   - `AGENTS.md` 의 `export PATH=…` 를 **먼저** 실행한다. 그래야 `pnpm`(corepack 셔임)을 찾는다.
   - `git` 은 `/Library/Developer/CommandLineTools/usr/bin/git` 을 쓴다. `/usr/bin/git` 은 Xcode 라이선스 때문에 막혀 있다.
   - 위험 31 이 해결되기 전까지는 `export TMPDIR="$(node -p 'require("fs").realpathSync(require("os").tmpdir())')"` 를 실행한다.
   - 브라우저 e2e 는 **키 없는 서버**(`LOCAL_NO_AUTH=1`)를 대상으로 돌린다. 키 인증 서버(`dev-up.sh`, 8790)로 돌리면 7건이 실패한다.
   - `timeout` 명령이 없다. 대신 `perl -e 'alarm 90; exec @ARGV' <명령>` 을 쓴다.

### 3.2 한 작업의 수명주기 — 순서를 바꾸지 않는다
1. **재현** — 문제를 실행으로 확인한다. 재현하지 못하면 "재현 불가"로 기록하고 고치지 않는다.
2. **원인 귀속** — 내 변경 때문인지 기존 결함인지 HEAD 에서 재현해 가린다(`git archive HEAD | tar -x -C <임시폴더>`). 기존 결함은 별도 항목으로 적는다.
3. **완료 기준 사전 등록** — 고치기 전에 §5 의 해당 행(또는 docs)에 "무엇이 어떻게 되면 완료인가"를 적는다. 모델이 관여하는 작업이면 반복 횟수와 통과 기준도 미리 적는다.
4. **수정** — 가장 작은 변경으로 고친다. 파일 jail·권한 검사·로그 가림 같은 안전 규칙을 약화해서 시험을 통과시키지 않는다.
5. **회귀 시험 + 결함 주입** — 새 시험은 수정을 되돌렸을 때 실패해야 한다. 되돌려 본 뒤에는 원복하고 `cmp` 로 원복됐는지 확인한다.
6. **관련 검증 실행** — 최소한 정적 4단계(`node scripts/verify-all.mjs`)를 돌린다. 서버·브라우저·보안 경계를 건드렸으면 해당 라이브 단계까지 돌린다. 출력은 `tail` 로 자르지 말고 파일로 남긴다.
7. **문서** — 해당 `docs/` 장, HANDOFF §7 상태, 이 문서 §5 상태를 갱신한다. 검증하지 못한 범위는 "미검증"이라고 쓴다.
8. **정리** — §3.5 를 따른다.
9. **커밋** — §3.4 를 따른다.

### 3.3 판정 규칙
- PASS 는 실행 결과로만 판정한다. SKIP, 0개 실행, 오래된 JSON, 다른 커밋의 결과는 PASS 가 아니다.
- 모델이 관여하는 결과는 **한 번 실행한 것으로 결론 내지 않는다.** 최소 3회, 가능하면 5회 실행하고 신뢰구간을 계산한다(`apps/verify/src/eval/stats.ts`).
- **같은 과제로 조정하고 같은 과제로 보고하지 않는다.** 기존 맥락 8과제, 대표 업무 3건, stage1 50문제는 이미 조정에 쓰였다. 앞으로 개선 효과는 새로 만든 보고용 과제로만 주장한다.
- 측정 도구가 실제로 무엇을 재는지 먼저 의심한다. 과거에 LTM 지연 측정은 Ollama 처리량을 재고 있었고, 보안 시험 실패는 macOS 임시 폴더 경로 때문이었다.
- 실패 이력은 지우지 않는다. 최신 성공만 남기지 않는다.
- 실제로 시험하지 않은 악용 가능성·실사용·다른 환경은 "등급", "추정", "미검증"으로 쓴다.

### 3.4 커밋·푸시
- 검증을 통과한 논리 단위 하나를 커밋 하나로 만든다. 메시지에는 무엇을 왜 바꿨는지와 검증 요약을 넣는다.
- 커밋하기 전에 staged 범위를 확인하고 비밀 패턴을 검사한다.
  ```bash
  /Library/Developer/CommandLineTools/usr/bin/git diff --cached -U0 | grep -nE 'sk-ant-[A-Za-z0-9_-]{20,}|sk-[A-Za-z0-9]{32,}|AIza[0-9A-Za-z_-]{35}|ghp_[A-Za-z0-9]{30,}|BEGIN [A-Z ]*PRIVATE KEY' || echo "비밀 패턴 없음"
  ```
- **푸시는 사용자에게 확인받은 뒤에만 한다.** 공개 저장소다. 푸시한 뒤에는 해당 SHA 의 CI 결과를 확인해 기록한다(`gh run list --repo jonghyun0000/aios --limit 5`).
- 공개 체험판(Vercel)은 푸시해도 갱신되지 않는다. 재배포는 사용자 승인 사항이다.

### 3.5 끝낼 때 정리 (HANDOFF §11 에 더해)
- 띄운 API 서버는 리스너 PID 가 아니라 `… --filter @aios/api dev` **래퍼 PID** 에 SIGTERM 을 보낸다. 리스너만 죽이면 래퍼와 tsx 가 고아로 남는다.
- 고아 esbuild 가 없는지 확인한다(HANDOFF §11 의 `comm` 기준 명령).
- Docker 는 자기가 만든 이미지와 컨테이너만 **ID 로 지정해** 지운다. 만든 적 없는 잔여물은 목록만 보고한다.
- 시험이 만든 대화는 **만든 id 로만** 휴지통에 보낸다(`PATCH /v1/sessions/<id>`, 본문 `{"deleted":true}`). 제목 패턴으로 일괄 처리하지 않는다.
- 맥 내장 디스크에 산출물이 남지 않았는지 확인한다. Playwright 결과 폴더는 예외다(HANDOFF 참고).

### 3.6 사용자에게 먼저 묻는 것 — 에이전트가 단독으로 하지 않는다

| 항목 | 이유 |
|---|---|
| 푸시, 공개 체험판 재배포, GitHub 저장소 설정 | 공개·외부 변경 |
| `phase8`, `--legacy-full`, 복원본을 운영에 전환, 옛 compose 볼륨(`1_pgdata`·`1_redisdata`) 삭제 | 파괴적 |
| 라이선스 선택 | 소유자 결정 |
| 외부 AI API 키 사용. 그 전에 과거 대화에 노출된 Anthropic 키를 폐기·재발급해야 한다 | 비용·보안 |
| macOS 계정 생성, `sudo xcodebuild -license`, 시스템 설정 | 에이전트 금지 작업 |
| 백업을 외부 매체나 클라우드로 보내기 | 데이터 반출 |
| 외부 사람 섭외 | 사람이 필요 |
| 평가표 배점·수용 조건 변경 | 목표를 옮기지 않는다 |

### 3.7 사용자에게 보고하는 형식
한국어로, 다음 순서로 쓴다: **결과 한 줄 → 한 일 → 실행으로 확인한 것(수치) → 확인하지 못한 것 → 발견한 기존 결함 → 사용자 결정이 필요한 것.** 과정 설명보다 결과를 먼저 쓴다.

---

## 4. 방향 — 사용자에게 한 번 확인할 것

단계 0·1 은 방향과 상관없이 먼저 한다. 단계 2·3 의 순서는 사용자가 이 제품을 무엇으로 쓰려는지에 따라 달라진다.

- **(가) 본인의 일상 도구로 쓴다** → 3-6 실사용 기록 → 2-3 독립 백업 → 3-1 eval 홀드아웃 → 2-1 CI 통합
- **(나) 공개 오픈소스·포트폴리오로 쓴다** → 라이선스 → 2-1 CI 통합 → 3-3 신규 사용자 관찰 → 2-2 클린룸 계정

답을 받기 전에는 두 방향에 공통인 **2-1 과 3-1** 부터 한다.

**확정(2026-09-22):** 사용자는 **(나) 공개 오픈소스·포트폴리오** 방향을 선택했다. 단계 0·1 완료 후에는 이 방향의 순서(라이선스 결정 → 2-1 → 3-3 → 2-2)를 따른다. 라이선스 선택과 신규 사용자 섭외·계정 생성은 §3.6에 따라 각각 사용자 결정을 먼저 받는다.

---

## 5. 로드맵

상태 값: `대기` / `진행` / `완료 (<커밋 SHA>)` / `보류 (<사유>)`

### 단계 0 — 인계 정리 (가장 먼저)

| # | 할 일 | 완료 기준 | 사용자 | 상태 |
|---|---|---|---|---|
| 0-1 | Fastify 5·감사 게이트 변경(커밋 `b9f12d9`, `docs/08-security.md` §15.5)과 이 가이드라인 커밋을 푸시하고 원격 CI 를 확인한다 | 푸시한 SHA 에서 GitHub 의 `audit`·`verify` job 이 모두 성공해야 한다. 새 `audit` job의 실제 원격 성공도 확인한다. 실패하면 로컬 결과와 무엇이 다른지부터 가린다 | 푸시 승인 | 완료 (`15218c0`, [CI audit·verify 성공](https://github.com/jonghyun0000/aios/actions/runs/35476338497), 2026-09-20) |
| 0-2 | HANDOFF §0 을 줄인다 | §0 은 30줄 이내의 "현재 상태"만 남기고, 날짜별 이력은 새 `CHANGELOG.md` 로 옮긴다. 옮기기 전후로 주요 수치와 커밋 SHA 가 어딘가에 남아 있는지 grep 으로 확인한다 | — | 완료 (`25557a6`, 실행 근거 `docs/30-container-build.md`) |

### 단계 1 — 깨진 것 고치기 (P0, 방향과 무관)

| # | 할 일 | 완료 기준 | 사용자 | 상태 |
|---|---|---|---|---|
| 1-1 | 운영 이미지 빌드 불가 (위험 30) | `docker build -f infra/Dockerfile --target api` 가 성공하고, 컨테이너를 띄운 뒤 `/healthz` 가 200 을 돌려준다. phase8 이 아닌 비파괴 방식으로 확인한다. engines 와 Dockerfile 베이스의 불일치를 잡는 CI 검사를 추가하고 결함 주입으로 확인한다 | — | 완료 (`25557a6`, 실행 근거 `docs/30-container-build.md`) |
| 1-2 | 검증 하네스 tmpdir (위험 31) | `TMPDIR` 를 우회하지 않고도 phase7 이 PASS 한다. 같은 패턴을 쓰는 다른 하네스는 실행 결과로 영향 여부를 가린다. 세부 완료 기준과 최초 재현은 `docs/32-tmpdir-harness.md`에 사전 등록했다 | — | 완료 (`ab31904`, 실행 근거 `docs/32-tmpdir-harness.md`) |
| 1-3 | API 오류 분류 (위험 32) | 클라이언트가 연결을 끊어도 error 레벨 로그가 0건이다. 잘못된 UUID 경로는 400 을 돌려준다. 둘 다 결함 주입으로 확인한다. 세부 완료 기준과 실행 근거는 `docs/33-api-error-classification.md`에 기록했다 | — | 완료 (`3a02beb`, `6107a70`; 실행 근거 `docs/33-api-error-classification.md`) |
| 1-4 | e2e 의 서버 전제 | `verify-all e2e` 가 키 인증 서버를 가리키면 실행 전에 BLOCKED 와 이유를 출력한다. 지금은 7건 실패로 나타나서 회귀처럼 보인다. 세부 완료 기준과 최초 재현은 `docs/34-e2e-server-precondition.md`에 사전 등록했다 | — | 완료 (`9107f28`, 실행 근거 `docs/34-e2e-server-precondition.md`) |
| 1-5 | `dev-up.sh` 의 조용한 실패 | `pnpm` 이 PATH 에 없으면 서버를 띄우기 전에 원인과 해결 방법(AGENTS.md 의 PATH)을 출력하고 실패한다. 지금은 "API 서버가 뜨지 않았다"만 나오고, 원인은 `/tmp/aios-api.log` 안에 묻힌다. 세부 완료 기준과 최초 재현은 `docs/35-dev-up-pnpm-preflight.md`에 사전 등록했다 | — | 진행 |

단계 1 의 1-1~1-3 은 2026-09-19 세션에서 데스크톱 앱의 작업 칩으로도 제안했다. 사용자가 칩으로 이미 진행했을 수 있으니 시작하기 전에 HANDOFF §7 과 `git log` 를 확인한다.

### 단계 2 — 두 번째 Mac 없이 재현성 확보 (§2)

| # | 할 일 | 완료 기준 | 사용자 | 상태 |
|---|---|---|---|---|
| 2-1 | B. Linux CI 통합 잡 | 새 러너에서 마이그레이션 → API 기동 → 대화 1회 응답 → 승인 흐름 1회 → readback 해시 일치까지 20분 안에 끝난다. 판정이 모델 품질에 좌우되지 않게 설계한다(모델은 응답이 있는지, 임베딩 차원이 1024 인지 정도만 판정). 마이그레이션 하나를 빼는 결함 주입에서 잡이 실패해야 한다 | — | 대기 |
| 2-2 | A. 클린룸 계정 | `docs/30-clean-account-install.md` 체크리스트를 쓴다 → 사용자가 계정을 만든다 → 수행 결과(막힌 곳, 걸린 시간, 문서 수정)를 기록한다. 막힌 곳마다 문서나 doctor 를 고친다 | 계정 생성, 30~60분 | 대기 |
| 2-3 | 독립 매체 백업·복구 | 사용자가 고른 매체(외장 디스크, 또는 암호화한 뒤 클라우드)에 통합 백업을 둔다. **T7 의 백업 사본을 쓰지 않았음을 증명할 방법을 먼저 적고**, 그 매체의 사본만으로 격리 복원 검사를 PASS 한다. HMAC 키를 어디에 보관하는지 문서화한다 | 매체 선택·전송 승인 | 대기 |
| 2-4 | C. macOS 러너 스파이크 | 가능한지 불가능한지 판정하고 근거를 `docs/` 에 기록한다(1시간 상한) | — | 대기 |

### 단계 3 — 근거의 질 높이기

| # | 할 일 | 완료 기준 | 사용자 | 상태 |
|---|---|---|---|---|
| 3-1 | eval 홀드아웃 + 다모델 비교 | 조정에 쓰지 않은 새 과제 20개 이상을 **먼저 커밋**하고 파일 해시를 기록한다. 그다음 qwen3:8b, exaone3.5:7.8b, llama3.1:8b(모두 T7 에 있음)를 각 5회 돌려 Wilson 95% 구간을 낸다. 결과를 보기 전에 프롬프트·정책을 고치지 않는다. eval 을 verify-all 에 넣을지(15~25분)는 이 결과를 보고 정한다 | — | 대기 |
| 3-2 | 시험이 0개인 패키지 | `plugin-host`(worker_thread 격리·권한 브리지, 단위 시험 0개. phase7 에 일부 통합 검사만 있다)부터 한다. 선언하지 않은 권한 호출 거부, 허용되지 않은 net host 거부, 시간 초과, 예외 격리를 계약 시험으로 만들고 결함 주입으로 확인한다. 이어서 `shared`·`sdk`·`cli` 를 한다 | — | 대기 |
| 3-3 | 신규 사용자 관찰 (D) | 1~3명에게 과제 3개(첫 대화 / 자료 연결 후 질문 / 파일 변경 승인·복구)를 맡긴다. 개입하지 않고 막힌 지점과 시간을 기록해 UX 백로그로 옮긴다 | 섭외 | 대기 |
| 3-4 | 완료 게이트·자기 검증의 실제 발동 (위험 15·16) | `eval/run.ts` 에 시스템 프롬프트 포함 옵션과 다중 턴 도구 과제를 추가한다. 게이트 발동을 실측해 "n회 중 k회 발동" 또는 "n회 중 관측 안 됨"으로 기록한다 | — | 대기 |
| 3-5 | 대형 저장소 색인 | 라이선스를 확인한 1만 파일 이상의 공개 저장소로 색인 시간·메모리·검색 p95 를 잰다. 기준은 미리 등록한다(지금까지 최대 60파일) | — | 대기 |
| 3-6 | 본인 실사용 기록 | 사용자가 1~2주 동안 실제 업무에 쓰면서 실패와 불편을 적는 양식을 `docs/` 에 만든다. 기록은 주 1회 백로그로 옮긴다 | 사용 | 대기 |

### 단계 4 — 사용자 결정을 기다리는 것 (먼저 하지 않는다)
- 라이선스 선택.
- OpenAI·Gemini·xAI·Claude 실호출 검증. 키를 재발급한 뒤에 한다.
- 공개 체험판 재배포.
- 옛 compose 볼륨 삭제.
- Docker 없이 네이티브 Postgres 로 도는 경로를 지원할지. 2-4 결과를 보고 정한다.
- (계속 관찰) bigdata SIGBUS(위험 14)는 별도 프로세스로 격리돼 있다. 재발하면 기록만 하고, 원인 규명은 따로 결정한다.

---

## 6. 점수와 문서
- 점수를 바꿀 때는 범주마다 근거 파일, 재현 명령, 해당 커밋 SHA, 실패 이력을 함께 적는다. 한 근거를 여러 범주에 중복해서 더하지 않는다.
- 자기 작업을 자기가 채점하지 않는다. 재평가는 구현과 분리된 검토가 맡고, §1 의 네 가지 반론에 각각 답해야 한다.
- §2 의 대체 근거는 점수에 더하지 않는다.
- 새 문서 번호는 `docs/30-` 부터 쓴다. README 의 "최근 검증 범위"에는 해당 커밋에서 다시 실행한 수치만 쓴다.
- 0-2 이후 HANDOFF §0 에는 현재 상태만 쓰고, 이력은 `CHANGELOG.md` 에 쓴다.

---

## 7. 이 프로젝트에서 실제로 있었던 실수 — 반복하지 않는다

| 있었던 일 | 교훈 |
|---|---|
| 프롬프트 규칙을 1회 성공만 보고 "효과 있음"으로 보고했다가, 3회 측정에서 기각됐다 | 반복해서 측정한다 |
| 같은 8과제로 조정하면서 18 → 23 → 24/24 가 됐다 | 보고용 과제를 따로 둔다 |
| 검증 하네스가 제품 코드의 약 60% 까지 커졌다 | 새 검사는 실제 결함을 잡을 때만 추가한다 |
| 운영 이미지가 일주일 동안 빌드되지 않았는데 아무도 몰랐다 | CI 가 돌지 않는 경로는 깨져 있다고 가정하고 확인한다 |
| 서버 전제가 다른 e2e 7건 실패를 회귀로 오인할 뻔했다 | 실패의 원인부터 가린다 |
| "30초 요약"이 약 100줄로 불어났다 | 이력은 CHANGELOG 로 보낸다 |
| 고아 esbuild 2개가 4일 반 동안 CPU 코어 약 7개를 점유해, 그 기간의 시간 측정이 오염됐다 | 끝낼 때 정리하고, 측정 전에 기기 부하를 확인한다 |
| `ps … grep` 이 자기 자신을 잡아서 대기 루프가 끝나지 않았다 | PID·`comm` 기준으로 확인한다 |
